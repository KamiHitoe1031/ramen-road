/**
 * サーバー側採点エンジン
 * Phase 3で public/js/utils/scoringEngine.js をNode.js用に移植する
 * クライアント版と同じロジックをサーバーで実行し、チート防止する
 */

// Phase 3で実装:
// const scoringData = require('../data/scoring.json');
// const ingredientsData = require('../data/ingredients.json');
// ... ScoringEngineクラスをそのまま移植

module.exports = {};
